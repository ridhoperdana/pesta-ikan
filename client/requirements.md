## Packages
framer-motion | Animations for UI elements (menus, game over screens)
lucide-react | Icons for UI
usehooks-ts | Utility hooks like useInterval, useWindowSize

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
